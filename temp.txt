  const myEstReward = totalGrossContributions > 0 ? rewardsInfo.currentRewardPool * (myContribution / totalGrossContributions) : 0;
                       <div className="text-xl font-bold text-white">₹{formatINR(Math.round(rewardsInfo.currentRewardPool))}</div>
